#Dado o array a, temos que encontrar o produto mínimo possível
#com o subconjunto de elementos presentes no array.
#O produto mínimo também pode ser um único elemento.

 
# def to find maximum product of a subset
def minProductSubset(a, n)
    if (n == 1) then
      return a[0]
  
    # Find count of negative numbers,
    # count of zeros, maximum valued
    # negative number, minimum valued
    # positive number and product
    # of non-zero numbers  
  
    
  end